# P039 battle-hud-effects findings

Generated from immutable Resource pin `519c35a293745b6a0477c4f6ea03110a89de2318` (tree `9a99b5163ca02ef04f82b9d3a3a246baa8a5e344`) and the exact R025/R026 evidence plus importer blobs on Gunny main `a0a677303cfa337a8921b960a78ad5f07375643d`. Both dependency records report `complete-with-explicit-unresolved` and `trackBDependencySatisfied: true`; every locked SHA-256 and Git blob SHA-1 matched before generation.

The package covers all 4,626 locked source paths (469,962,503 bytes; 4,555 unique blobs): 892 exact, 0 inferred and 3,734 unresolved. It catalogs 889 existing browser-native rasters without copying their raw bytes. HUD-control families and transient-effect families are separate lexical groups only; P039 claims no gameplay or runtime selection authority.

FFDec 26.2.1 was verified by its pinned CLI and JAR hashes. From the byte-identical `partical/shape.swf` pair it extracted 96 real PNG textures. The byte-identical particle XML pair contains 83 emitter records and 103 particle definitions. Exactly 100 definitions map to one of 94 `ParticalShap` symbol classes; 96 have an extracted bitmap texture, four mapped definitions use vector-only symbols and remain browser-native-pending, and display creators 0, 72 and 73 have no exact symbol class and remain unresolved. Duplicate emitter IDs 0 and 80 are preserved by source index rather than silently merged.

Exact Track A metadata is retained for 3,300 real SWF timelines, including FPS, frame counts/duration, display depths, masks, blend counts, transforms, bounds, frame labels, states and lifecycle evidence. Metadata is not presented as visual conversion: SWF runtime, CSS reconstruction and synthetic animation are forbidden, and every unconverted visual stays fail-closed behind the existing immutable Gunny fallback.

The package commit is `6b99cd50a85c60a71b5cde66c56d6bf99c6dc4d0` with tree `307a418f8c250ce3c44666969fe97b5f08ddcae9`. Node syntax, the exact-root P039 checker, the aggregate Track B verifier, all generated JSON/media hashes, complete source-path census, locked-path scope, `git diff --check`, and the canonical raw-source cleanliness check passed locally. GitHub Actions were not used or inspected. Runtime integration remains reserved for R039 after the immutable Resource merge.
