# Resource Package P047 findings — audio and XML

- Package session: `P047`
- Runtime handoff: `R047`
- Owner: `audio-localization`
- Raw source: `trinhtanphat/Resource@519c35a293745b6a0477c4f6ea03110a89de2318`
- Track A dependency: `trinhtanphat/Gunny` session `R035`
- GitHub Actions: not used or inspected

## Result

P047 inventories every locked audio/XML input with `ffprobe`, extracts or
converts browser-safe media deterministically, and publishes canonical XML
bytes from the pinned Git tree. The package is evidence-complete but does not
claim runtime integration or semantic authority that the source does not
provide.

| Measure | Result |
|---|---:|
| Raw source files | 229 |
| FLV paths / unique FLV blobs | 222 / 202 |
| Lossless MP3 extraction groups | 200 |
| AAC-to-Ogg conversion groups | 2 |
| Unique browser audio outputs | 204 |
| Browser video derivatives | 4 |
| Embedded SWF audio outputs | 2 |
| Canonical XML documents | 2 |
| Localization catalogs | 0 |
| Exact / inferred consumer mappings | 9 / 2 |
| Unresolved consumer mappings | 213 |
| Browser runtime integration | no |

## P047-F001 — MP3 streams remain byte-exact

- Status: fixed
- Severity: critical
- Category: codec-preservation
- Two hundred unique FLV groups carry MP3 audio. Their elementary MP3 streams
  are extracted losslessly; the checker rejects any changed output digest,
  codec metadata, duration mapping or source membership.

## P047-F002 — AAC audio requires deterministic Ogg conversion

- Status: fixed
- Severity: high
- Category: codec-conversion
- The two H.264/AAC FLV groups are converted to deterministic Ogg Vorbis audio.
  Their separate MP4 video derivatives preserve the multimedia payload without
  treating an audio event name as proof of a video consumer.

## P047-F003 — embedded SWF audio is provenance-extracted

- Status: fixed
- Severity: critical
- Category: embedded-audio
- `sound201.swf` is extracted with FFDec and deterministically converted from
  SWF ADPCM to Ogg with exactly 32,935 samples.
- `sound202.swf` is extracted as its exact embedded MP3 stream with 50,799
  samples. Both outputs retain source, symbol and toolchain provenance; neither
  SWF is published as a runtime container.

## P047-F004 — four multimedia sources are browser-safe but unresolved

- Status: fixed for format; open for consumer ownership
- Severity: high
- Category: multimedia
- Four FLV sources receive MP4 derivatives: two H.264/AAC and two VP6/MP3
  inputs. The browser-safe bytes are published, but P047 found no authoritative
  non-audio runtime owner, so R047 must keep all four video consumers inactive.

## P047-F005 — XML bytes must come from the pinned Git blobs

- Status: fixed
- Severity: high
- Category: deterministic-source
- Windows checkout conversion changes the byte representation of
  `xml/config.xml`. P047 reads both XML documents from the pinned Git tree, not
  from mutable working-tree line endings, and verifies their canonical digests.
- `config.xml` is a 20-emitter particle configuration. It is not a localization
  catalog. P047 found zero localization catalogs and makes no translation-data
  claim.

## P047-F006 — `ddtluncher.png` is an AIR installer, not a PNG

- Status: fixed for classification; browser runtime forbidden
- Severity: critical
- Category: legacy-launcher
- `xml/update/desktopupdate.xml` references `ddtLuncher.png`, but the pinned
  source filename differs by case and the bytes are an Adobe AIR installer ZIP,
  beginning with the AIR installer MIME entry rather than a PNG signature.
- P047 publishes the object only as a content-addressed `.air` support artifact.
  R047 must never load, parse, execute or present it as browser media.

## P047-F007 — event names do not resolve consumer ownership

- Status: open
- Severity: critical
- Category: runtime-owner
- Static source authority resolves nine event-to-file mappings exactly and two
  dynamic world-boss mappings by an explicit filename pattern. The remaining
  213 consumers, including both embedded SWF events and all non-audio video
  consumers, remain unresolved.
- Decision: R047 may activate only consumers proven by an authoritative runtime
  owner. It must preserve every unresolved mapping as fail-closed.

## P047-F008 — package completion is not R047 completion

- Status: open
- Severity: high
- Category: runtime-integration
- P047 publishes immutable browser bytes, codec/XML inventories, and a
  fail-closed contract. It does not implement browser playback or XML parsing.
- R047 must pin the immutable Resource merge commit, route all URLs through the
  central resolver, preserve whole-file loop and global music-volume semantics,
  parse the two XML documents with the browser XML parser, and prove that no
  FLV/SWF/AIR runtime path remains.
