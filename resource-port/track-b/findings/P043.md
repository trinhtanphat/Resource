# P043 store-mail-economy findings

Generated from immutable Resource pin 519c35a293745b6a0477c4f6ea03110a89de2318 (tree 9a99b5163ca02ef04f82b9d3a3a246baa8a5e344) and the exact R030/R031 evidence, importer and shard blobs on Gunny main e2552ddf57beca13d9ab2dca1b168fda0161f337. The two importers independently reprocessed the clean raw pin and reproduced R030 at 4,096 files / 29,627,845 bytes and R031 at 639 files / 5,567,858 bytes.

The package catalogs all 4,735 assigned paths and 35,195,703 source bytes across image/card, image/cardbox, image/gift, image/oneshopping and image/unfrightprop. Track A classified 4,631 paths exact and 104 unresolved. P043 safely recovers 78 unresolved aliases through byte-identical exact source objects and maps 15 valid raster paths to 12 immutable content-addressed outputs after signature, SHA-256 and dimension checks. Package coverage is therefore 4,724 exact browser-native mappings with 11 source objects explicitly unresolved.

The remaining unresolved set is fail-closed: four truncated PNG files, four objects detected as SWF without a valid SWF signature or known Gunny wrapper, and three zero-byte objects. No SWF or FLA container is published and P043 claims no legacy visual or behavior port.

Typed outputs cover 216 card-art mappings across 132 exact source IDs, 84 card-box mappings across 84 IDs, 142 gift presentation assets, 15 shop decoration assets and 4,267 item-icon mappings. Shared directory identifiers are retained as source identity only. They are not treated as display labels, rarity tiers, prices, currencies, inventory records or transaction rules.

The existing Gunny shop, gift, mail and store-embed foundation is pinned at 12 exact code/manifest blobs and 195 asset files totaling 1,517,862 bytes. P043 references that foundation without binary duplication. Selected, hover and disabled presentation states are supported only where the pinned client code and assets prove them. Rarity presentation reads the Worker/API item field; it is never inferred from card or icon artwork. The five P043 raw source families contain no reviewed mail-surface family, so the exact 57-file existing Gunny mail foundation remains the referenced presentation source rather than being replaced with unrelated art.

Price, currency, rarity, inventory, gifting, mail lifecycle, recipients, permissions and settlement remain Gunny Worker/API authority. Decorative art is separated from transaction semantics in every catalog and contract. Runtime consumption, same-origin package routing and live lifecycle wiring remain reserved for R043.

Track A publication adds the two exact R030/R031 boundary manifests (4,959 bytes total) under the locked store-mail-economy prefix, with hash-bound root and session receipts. It does not reinterpret the 11 unresolved package objects or infer any price, currency, rarity, inventory, recipient, permission or settlement behavior from artwork.

The package commit is `1868dad5f13d31e25036e7488e9447e911ecd063` with tree `15e061d48e39c3f1826c5a98f4aec4e0f96d6c6d`. The package-local .gitattributes disables text normalization; all 24 manifest descriptors were also verified directly from the Git index with zero byte/hash mismatches. Node syntax, exact-root/context generation, standalone package verification, aggregate Track B verification, dependency importer checks, source path census, raster signature/dimension checks, foundation identity, locked-path staging scope, `git diff --check` and the Track A publication check passed locally. GitHub Actions were not used or inspected.
