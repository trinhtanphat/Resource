# P044 quests-events findings

Generated from immutable Resource pin `519c35a293745b6a0477c4f6ea03110a89de2318` (tree `9a99b5163ca02ef04f82b9d3a3a246baa8a5e344`) and the exact R032 evidence, importer and quests-events shard blobs on Gunny main `85a40447689a056ba0135331cbbfed8d52f0ac25`. The R032 importer independently reprocessed the read-only source roots and reproduced 774 files / 33,541,657 bytes / 699 exact / 75 unresolved. P044 then rehashed every assigned pinned object and all 55 duplicated weekly weapon objects.

Three materialized paths differ from their pinned Git objects: `image/task/item1/c98.php`, `image/task/item1/c99.php` and `image/weekly/weeklyinfo.xml`. This is the provenance condition already recorded by R032. P044 does not copy or modify those materialized files; it reads and verifies their immutable Git blobs by object ID, byte count and SHA-256. The raw Resource directories remain read-only.

The package catalogs all 774 assigned paths across badgerole, badge, binglong, cardcollect, collectiontask, ddqiyuan, dice, explorermanual, honorbackground, leaguerank, sevendouble, task, weekly and yearfood. P044 retains the 699 Track A exact results, safely recovers three valid raster objects whose filename or path representation prevented direct publication, and reaches 702 exact browser-native mappings with 72 objects explicitly unresolved. The three corrected objects total 144,489 bytes: two JPEG payloads are published with a `.jpg` suffix and the Unicode task PNG is published under a content-addressed ASCII filename. Their signatures, dimensions, byte counts and SHA-256 values are gated by the checker.

All 701 raster records are visual-ready and behavior-ready remains zero. Every visual has an explicit consumer role and identity; nearby filenames, directory numbers and artwork are not used to infer quest IDs, rewards, dates, eligibility, resets, schedules or progression. `image/ddqiyuan/` contributes no R032 source file, so P044 invents no placeholder asset or behavior for it.

The 64 SWF records remain metadata-only evidence generated with FFDec 26.2.1. Fifty-five are exact duplicates of the P038-owned unresolved weekly weapon SWFs, while nine belong to collectiontask, sevendouble or yearfood. The sevendouble audio container exposes five `DefineSound` tags, but extraction, codec semantics and playback triggers are not authoritative, so it remains disabled. P044 publishes no SWF runtime, script, PHP, BAT, RAR or other quarantined binary payload.

The pinned `weeklyinfo.xml` contains 19 historical publication entries, but none resolves to an exact source path in the R032 shard. All 19 remain disabled. Dates, target pages and legacy paths in that XML are retained as source metadata only and are not treated as active server authority.

The existing Gunny foundation is pinned at eight code files and 186 presentation assets totaling 1,111,359 bytes. It proves the current `/api/quests`, `/api/daily`, `/api/daily/claim` and `/api/live-events` client boundaries plus Worker-side quest claims and scheduled live-event operations. P044 references this foundation without duplicating it. Quest progress, claims, rewards, calendar reset state, event dates, eligibility and scheduling remain Gunny Worker/API authority; runtime package consumption is reserved for R044.

The submitted package commit is `4c639dd61718e2fbd20012cf41c706e2a1c2801e` with tree `e7197952fc6d50771fa5e5b66fd8a45637f1bed2`. Node syntax, the exact R032 evidence/importer/shard pins, immutable Resource commit/tree identity, raw-source hashes, corrected raster signatures and dimensions, weekly duplicate identities, FFDec metadata, consumer mappings, Gunny foundation identity, standalone package verification, aggregate Track B verification, locked-path staging scope and `git diff --check` passed locally. GitHub Actions were not used or inspected.
