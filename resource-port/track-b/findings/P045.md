# P045 hall-room-world findings

P045 reviews the complete `hall-room-world` Track A surface from immutable Resource commit `519c35a293745b6a0477c4f6ea03110a89de2318` (tree `9a99b5163ca02ef04f82b9d3a3a246baa8a5e344`). The R033 dependency is refreshed and pinned at Gunny commit `c6ed10b54c9fe79ab9fc3f307593210f9e855b95`, evidence blob `d3d49478e7daad52564f69e58bac522d0b8cbf82`, and importer blob `7bbdc3321e1cf46d2d99c2756aa72e231370d237`.

## Result

- Source census: 396 files / 48,720,359 bytes.
- Raster catalog: 311 references / 156 unique blobs, addressed through the R033 external R2 staging package; P045 duplicates zero raster bytes.
- Timeline census: 101 occurrences / 97 unique SWF blobs, including nine embedded occurrences from eight legacy zlib bundles.
- Browser-native visual conversions: 73 timelines / 110 content-addressed artifacts.
- Explicit unresolved dispositions: 20 visual timelines plus four audio-only timelines handed to P047/R047.
- Navigation: one `DefineButton2` candidate, zero enabled hotspots and zero transitions.
- Behavior-ready timelines: zero. Runtime integration remains owned by R045.

The submitted P045 package tree is `1958ba4f734572c8335ca7af39728c0cfdb8673e`. The eventual rebase result, not this branch or a self-referential field, is the immutable release commit that R045 must pin.

## P045-F001 - all 97 SWF blobs receive a disposition

P045 exports scripts for all 97 unique blobs with FFDec 26.2.1, records source stage, symbols, sprites, labels, placements, transforms, bounds, interactions and script statistics, and runs the full review twice. Review JSON and every retained media byte reproduce exactly across the two runs.

The 63 fully static timelines have one authoritative symbol, one-frame child sprites, no script behavior, no button/clip action and no mask. They become exact SVG artifacts. Six escort timelines retain the authored labels `stand`, `transparent`, `accelerate` and `moderate` as 30 lossless WebP variants. Frame 36 is retained as its exact transparent, non-looping unreachable tail frame rather than being folded into a labeled state.

The faction-war entry icon retains the complete 180-frame least-common nested cycle. World Boss 2 retains its 60-frame first state and exact one-frame second state; the second state is not padded into a fake 60-frame animation. Two embedded monster timelines retain all seven exported action symbols each, producing 14 lossless WebP action artifacts. Every WebP frame is VP8L and no lossy `VP8 ` frame is accepted.

## P045-F002 - 20 visual timelines remain fail-closed

`campbattlemap.swf` exposes one `DefineButton2`, but aggregate R033 bounds do not bind a per-button rectangle and P045 owns no authoritative handler. Its hotspot therefore remains disabled. `campbattlepassage.swf` retains nested periods from 2 through 88 frames without a compact complete cycle.

Both 3208x2000 faction-war maps have nested 8/9-frame cycles. A reviewed 72-frame full composite exceeded the local memory budget, so P045 retains no partial or downsampled substitute. The World Boss 4 icon would require a reviewed 1,980-frame combined cycle and remains unresolved.

The remaining catch-insect, Christmas, world and world-boss scene/icon/map timelines depend on external classes, dispatched events, or nested behavior not owned by this package. P045 records their exact metadata and reasons but publishes no SWF runtime, representative screenshot, invented frame, invented coordinate or invented gameplay behavior.

## P045-F003 - audio ownership moves intact to P047

Four unique audio-only timelines are reviewed but publish no P045 audio bytes: direct `image/escort/escortaudio.swf` and three unique embedded monster sound blobs. A repeated export probe showed that FFDec's PCM decode of `escortSound05` changes sample bytes between runs even though the source bitstream is stable. MP3 passthroughs are deterministic, while that sound is retained by FFDec as an FLV-contained legacy codec.

P047/R047 therefore owns the complete ffprobe census, unsupported-codec conversion, loop points, volume, event mapping and provenance for all four timelines. P045 does not split audio ownership or call a partially decoded container complete.

## P045-F004 - Hot Spring is audited, not fabricated

The P045 source shard contains no Hot Spring file. Gunny commit `96a86885e23e5028e6654ed5b029870151b32d73` already supplies the room-list/world foundation and 132 exported assets totaling 7,589,533 bytes, but its own fidelity record remains non-pixel-exact and identifies missing vector shapes, sprite timelines, scene masks, collision and water masks.

P045 references that immutable foundation without duplicating artwork, inventing missing geometry, enabling new hotspots or taking over Gunny room/gameplay authority.

## Validation

- R033 external importer check: pass, 396 files / 48,720,359 bytes / 97 unique SWF summaries.
- P045 deep generation and independent reproduction: pass.
- Standalone P045 checker: pass.
- Aggregate Track B verifier: pass for 11 package sessions, 11 runtime handoffs and 36 dependency edges.
- Media validation: 63 SVG and 47 lossless WebP artifacts; prohibited SWF/FLV/EXE/JAR output count is zero.
- Visual inspection: escort, faction-war, embedded monster and World Boss state samples render as expected.
- Locked-path staging and `git diff --check`: pass.
- GitHub Actions: not used or inspected.
