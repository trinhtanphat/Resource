# P041 guild-social-marriage findings

Generated from immutable Resource pin `519c35a293745b6a0477c4f6ea03110a89de2318` (tree `9a99b5163ca02ef04f82b9d3a3a246baa8a5e344`) and the exact R028 evidence/importer blobs on Gunny main `0bc3c4b14ff44d1da162eadb4f338610ac3d4d5d`. The R028 importer independently reprocessed all 1,201 assigned files from the clean raw pin and reproduced the locked 18,391,659-byte census before publication.

The package catalogs every assigned source path: 1,201 files, 18,391,659 bytes and 1,031 unique blobs. Track A classified 1,173 objects exact and 28 unresolved. P041 preserves every source byte while correcting two media extension mismatches in browser-native exports: `image/church/map/13/samll_map_s.jpg` is a 131 by 51 PNG, and `image/consortiamap/default.png` is a 1,192 by 533 JPEG. The corrected copies are byte-identical to their raw sources and raise exact browser-native coverage to 1,175, leaving 26 SWF timelines explicitly unresolved.

The package publishes full catalogs for 986 wedding-appearance rasters, 141 church map files across 30 map IDs, and 46 exact ceremony-presentation rasters. It does not invent character layer order or attachment anchors because R028 contains no authoritative compositor contract for them. Filename-only runtime selection is prohibited.

Typed geometry and presentation behavior are pinned to the reviewed DDTank41 church-room sources and the existing Gunny R028 foundation. Eight immutable FFDec-compatible foundation outputs were hash-, dimension- and blob-verified at Resource commit `7dc16b70b4b868d6be709cca7d400def67f6d4b6`; none is duplicated into P041. The contract preserves source coordinate transforms, camera clamping, collision-mask hit testing, member depth order, toolbar/menu states and authenticated leave/kick/forbid boundaries. Business state, permissions, guild lifecycle and currency settlement remain server-owned.

All 26 SWF containers retain exact Track A/FFDec metadata, but P041 claims zero SWF behavior ports and zero browser-native SWF visual compositions. Missing map00 composition, per-child Flash depth, PathRoboSearcher segmentation, invite/continuation frames, wedding start/stop, fireworks, gun salute, gift handling and settlement remain fail-closed. SWF runtime, fabricated animation, representative-frame animation claims and client business authority are forbidden.

The package commit is `41f0fd0f1ee73b4e6d93773f240b6392fd4abdae` with tree `724bf297a9f539050a3e12a8b4dda447fd660b7c`. Node syntax, the exact-root/context P041 checker, aggregate Track B verifier, generated JSON parsing and hashes, complete source-path census, two media signature/dimension checks, immutable foundation audit, locked-path scope, `git diff --check`, canonical raw-source cleanliness and the R028 importer check passed locally. GitHub Actions were not used or inspected. Runtime integration remains reserved for R041 after the immutable Resource merge.
