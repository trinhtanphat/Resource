# Resource Package P046 findings — common UI

- Package session: `P046`
- Runtime handoff: `R046`
- Owner: `common-ui`
- Raw source: `trinhtanphat/Resource@519c35a293745b6a0477c4f6ea03110a89de2318`
- Track A dependency: `trinhtanphat/Gunny` session `R034`
- GitHub Actions: not used or inspected

## Result

P046 publishes the 47 `image/interfaceicons/` assets as a deterministic,
content-addressed browser raster package. Forty-six outputs reuse the exact
source PNG blobs. `icon1.png` uses only the R034-proven repair: remove the
five-byte legacy prefix and restore the corrupt IHDR width byte from `ff` to
`00`, with stored/restored IHDR CRC-32 `2b0e3243`.

| Measure | Result |
|---|---:|
| Source files | 47 |
| Source bytes | 278,072 |
| Browser bytes | 278,067 |
| Identity PNG | 46 |
| CRC-proven adapted PNG | 1 |
| Exact asset selections | 47 |
| Inferred visual-family labels | 47 |
| Resolved runtime consumers | 0 |
| Unresolved runtime consumers | 47 |

## P046-F001 — immutable package replaces the removed Gunny public payload

- Status: fixed
- Severity: critical
- Category: publication
- Gunny removed `public/assets/resource-port` as an invalid bulk-asset
  destination and requires immutable Resource/jsDelivr publication.
- Resolution: P046 owns the browser package in `trinhtanphat/Resource`.
  Production consumers must pin the eventual immutable merge commit. A mutable
  `main` or `web-assets` URL is never accepted as release evidence.

## P046-F002 — `icon1.png` requires deterministic legacy repair

- Status: fixed
- Severity: critical
- Category: legacy-format
- Source prefix: `00035e5f5e`
- Source SHA-256:
  `1453231f6a011cf0fd55170b9f134ac7a79001b3bb83273ba108a36994b7789e`
- Browser SHA-256:
  `bc43679bb101481ddbfba1f166f78bf22b6dd01e4c9c925bd1a436a78af35139`
- The P046 checker rejects any different prefix, patch, size, digest or IHDR
  CRC result.

## P046-F003 — visual identity is not runtime ownership

- Status: open
- Severity: critical
- Category: runtime-owner
- All 47 assets are visually consistent with legacy hall/interface shortcut
  icons, but numeric filenames and embedded bitmap labels do not prove the
  semantic name, click handler, order, availability gate, state or lifecycle.
- Decision: the package records the visual family as `inferred`; every
  consumer mapping remains `unresolved`. R046 must find an authoritative source
  registry/config/request and handler before making an icon interactive.

## P046-F004 — package completion is not R046 completion

- Status: open
- Severity: high
- Category: runtime-integration
- P046 publishes bytes and a fail-closed consumer contract only.
- Gunny R046 must pin the immutable Resource commit, route URLs through the
  central asset resolver and preserve all unresolved consumers as
  non-interactive.
