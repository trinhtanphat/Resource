# P042 pet-farm findings

Generated from immutable Resource pin `519c35a293745b6a0477c4f6ea03110a89de2318` (tree `9a99b5163ca02ef04f82b9d3a3a246baa8a5e344`) and the exact R029 evidence/importer blobs on Gunny main `0f633d66fc828c311dfd3e984a4d5a7e4c4232a5`. The R029 importer independently reprocessed all 1,700 assigned files from the clean raw pin and reproduced the locked 29,109,140-byte census before publication.

The package catalogs every assigned source path: 1,700 files, 29,109,140 bytes and 1,096 unique blobs. Track A classified 1,285 objects exact and 415 unresolved. P042 validates and safely addresses 220 additional byte-identical PNG source paths (214 declared as JPG and six unsafe percent-encoded paths), represented by 140 unique content objects. Package browser-native source coverage is therefore 1,505 exact objects with 195 source objects explicitly unresolved.

P042 extracts 62 unique authored ZIP packs under strict local/central-directory, deflate, uncompressed-size, CRC32 and path-safety checks. Their 164 exact entries comprise 62 PNG atlases, 62 TexturePacker XML files and 40 DragonBones-compatible rig JSON files: 1,522 authored subtextures, 84 armatures, 991 slots and 124 named animations. The complete original XML and rig JSON bytes are retained. Static pet stage icons are cataloged as static presentation and never claimed as animation. Mount atlas rectangles and trim geometry are authored; mount playback timing remains unresolved because those archives contain no authoritative frame rate.

One source defect remains visible rather than rewritten: horse 122 names `hourse.png` in its XML while the verified archive entry is `horse.png`. The package preserves the XML byte-for-byte and supplies an explicit `runtimeImageOutput` mapping to the checked `atlas.png`. Package-local Git attributes disable text normalization and allow authored CRLF at end-of-line, so archive entry hashes remain stable across Windows checkouts.

Typed catalogs cover 68 pet species and 225 stage icons, 51 farm crops and 204 authored seed/crop-stage rasters, 197 pet equipment icons across arm/cloth/hat slots, all 761 pet-skill source objects, mount cloth/saddle presentation layers, exact follow-rig actions/states/timelines and fail-closed fallback behavior. Farm images do not claim growth duration; skill icons do not claim labels or combat effects; filenames and nearby IDs do not authorize invented behavior.

The existing Gunny PetsBag foundation is pinned at five exact blobs. Its 31 reviewed outputs, active `standA` timeline and Worker-owned `/api/pets` boundary are referenced without binary duplication. Its FFDec-derived fidelity remains explicitly non-pixel-exact. Business state, inventory, feeding, growth, equipment permissions and combat remain Gunny Worker authority.

All 40 SWF-like objects remain disabled. Twelve retain exact FFDec metadata; 28 extension/magic mismatches have no FFDec timeline claim. P042 claims zero SWF visual conversions, zero SWF behavior ports, no SWF runtime and no client business authority. Two corrupt PNGs, 29 `thumbs.db` files, one empty file, three RAR files and 14 unknown WD3 objects also remain unresolved.

The immutable package commit is `cbd73b155b4ee19deff138d656a70c20da68dd9f` with tree `88786d91ef7e0936511d5f93a6cbf7046b1e22fa`. Node syntax, exact-root/context P042 generation and verification, aggregate Track B verification, complete source-path census, package artifact hashes, authored archive integrity, source/media dimensions, locked-path scope, `git diff --check`, canonical raw-family cleanliness and the full R029 importer check passed locally. GitHub Actions were not used or inspected. Runtime integration remains reserved for R042 after the immutable Resource merge.
